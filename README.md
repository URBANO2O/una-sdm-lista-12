Solução:

(Bloqueio Otimista): O sistema verifica se a "versão" do produto ainda é a mesma na hora de salvar. Se outro cliente comprou antes, a versão mudou, e o sistema cancela a segunda venda automaticamente.

